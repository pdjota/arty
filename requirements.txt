# Art classification — CNN-RNN (ResNet-50 + BiLSTM), WikiArt
torch>=2.0
torchvision>=0.15
datasets>=2.14        # HuggingFace streaming dataset access
numpy>=1.24
Pillow>=10.0
pandas>=2.0
scikit-learn>=1.2
matplotlib>=3.7
tqdm>=4.65
huggingface_hub>=0.25.0,<1.0  # Gradio 5.x needs HfFolder; removed in hub 1.0
pytest>=7.0          # tests
pytest-cov>=4.0      # coverage

# Gradio demo (HF Space uses root requirements.txt)
# 5.50+ includes gradio_client fix for Pydantic 2.11 JSON Schema (additionalProperties bool) /api crash
gradio>=5.50,<6

# Minimal deps to run the Gradio demo from ./gradio (repo ../src still required).
torch>=2.0
torchvision>=0.15
Pillow>=10.0
huggingface_hub>=0.25.0,<1.0
# 5.50+ includes gradio_client fix for Pydantic 2.11 JSON Schema (additionalProperties bool) /api crash
gradio>=5.50,<6
